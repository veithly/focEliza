{
  "name": "@elizaos/plugin-mina",
  "version": "0.1.0",
  "description": "Mina Protocol plugin for ElizaOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rimraf dist",
    "prepare": "npm run clean && npm run build"
  },
  "dependencies": {
    "@elizaos/core": "workspace:*",
    "o1js": "^0.17.0",
    "typescript": "^5.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.0"
  },
  "peerDependencies": {
    "@elizaos/core": "workspace:*"
  }
}